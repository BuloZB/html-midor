## ######################## ##
##           SASS           ##
## ######################## ##

guard 'compass', :configuration_file => 'sass.config.rb' do
	watch(%r{^sass/.+\.s[ac]ss})
end

guard 'livereload' do
  watch(%r{css/.+\.css})
  watch(%r{images/.+\.(gif|jpg|png)})
end
